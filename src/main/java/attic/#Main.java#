
package languish;

import org.antlr.runtime.*;
import org.antlr.runtime.tree.*;

public class Main {
    public static void main(String[] args) throws Exception {
	System.out.println(
			   Evaluator.evaluate(
					      newnew LInteger(3)).toString());
	

	/*        ANTLRInputStream input = new ANTLRInputStream(System.in);
	LanguishBaseLexer lexer = new LanguishBaseLexer(input);
        CommonTokenStream tokens = new CommonTokenStream(lexer);
        LanguishBaseParser parser = new LanguishBaseParser(tokens);
        LanguishBaseParser.prog_return r = parser.prog();
	*/
        // walk resulting tree
	//        CommonTree t = (CommonTree)r.getTree();
        //CommonTreeNodeStream nodes = new CommonTreeNodeStream(t);
	//        Eval walker = new Eval(nodes);
        //walker.prog();
    }
}
