export <module>;

token word ($filename) {
  part: 'a' .. 'z' | 'A' .. 'Z' | '_' | '/' | '.';
  keywords: infer;
}

token regex {
  langdec -> "#lang std;;\\n"
}

token regex {
  body -> '\.'*
}

<module>
  TUPLES -> $langdec $body
;

<filename> 
  FILE -> $filename
  BUILTIN -> "__BUILTIN__"
;
